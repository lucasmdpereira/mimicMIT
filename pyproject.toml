[project]
name = "mimic"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "makefile>=1.1.0",
    "pip>=25.1.1",
]

[tool.uv.workspace]
members = ["MIMIC-IV-Data-Pipeline"]
